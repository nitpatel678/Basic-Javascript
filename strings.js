let a = "nitin";
console.log(a,a.length);
console.log(a[1]); 

console.log(a.toUpperCase());
console.log(a);
b = a.toUpperCase(); // store upper case all character in new variable
console.log(b);

s3 = a.trim(); // trim the strings
console.log(s3);

// indexof('a') function to find the index of character
// lastIndexOf('a') to pick the last index of character 


// slice() is used to slice string , we can also set index to get particular string part
